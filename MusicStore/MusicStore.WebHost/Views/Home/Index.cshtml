@model IEnumerable<Album>
@{
    ViewBag.Title = "Index";
}

<h3><em>Fresh</em> off the grill</h3>

<ul class="albums-list">
    @foreach (var album in @Model)
    {
        <li>
            @Html.DisplayFor(x => album, "AlbumLink")
        </li>
    }
</ul>
